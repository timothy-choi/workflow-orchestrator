{
  "name": "failure-retry-demo",
  "description": "Step fails by exiting non-zero; orchestrator retries up to maxRetries. Local mode runs the shell command; Kubernetes mode uses the worker callback.",
  "steps": [
    {
      "name": "flaky",
      "image": "busybox:latest",
      "command": "echo failing-on-purpose && exit 1",
      "dependencies": [],
      "timeoutSeconds": 120,
      "maxRetries": 3
    }
  ]
}
